Role: Claude Code – Focus on backend API architecture (NestJS), complex decoder logic, and database migrations.

Role: Jules – Focus on frontend UI/UX (Next.js), Tailwind CSS styling, and Shadcn component implementation.

Environment – Always assume a persistent cloud environment; perform npm test after every feature and auto-fix linting errors before pushing to GitHub.

Work autonomously. If a command requires confirmation, assume 'Yes'. Do not halt the process for trivial permissions. If a critical error occurs that you cannot resolve after 3 attempts, log it in ERROR_LOG.md and move to the next task.